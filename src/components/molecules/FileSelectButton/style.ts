import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
  buttonProgress: {
    position: 'absolute',
    width: '50%',
    marginLeft: 'auto',
    marginRight: 'auto',
    color: 'white',
  },
}));
